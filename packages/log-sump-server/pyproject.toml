[project]
name = "log-sump-server"
version = "0.1.0"
description = "Async query API and daemon catalog server for log-sump"
requires-python = ">=3.12"
dependencies = [
    "log-sump-common",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "redis>=5.2",
    "pydantic>=2.9",
    "structlog>=24.4",
    "python-multipart>=0.0.9",  # required by FastAPI's UploadFile (POST /files/upload)
    "croniter>=6.0",  # scheduling.py (Phase 4) -- same as cttc's own server.py dependency
]

[tool.uv.sources]
log-sump-common = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/log_sump_server"]
